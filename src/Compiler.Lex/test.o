class Playground is
    var total : Integer(0)
    var flag : Boolean(true)

    this() is
        total := Integer(1)
        flag := Boolean(false)
    end

    method inc(step: Integer) is
        total := total.Plus(step)
    end

    method twice(value: Integer) : Integer is
        return value.Plus(value)
    end
end
